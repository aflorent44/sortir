<header class="text-gray-600 body-font bg-[#579FB5]">
    <div class="container mx-auto flex flex-wrap p-5 flex-col md:flex-row items-center text-gray-100">
        <nav class="flex lg:w-1/3 lg:justify-start flex-wrap items-center text-base ">
            {% if app.user %}
            <a href="{{ path("app_event_index") }}" class="mr-5 hover:text-gray-900 ">Sorties</a>
            {% endif %}
            {% if app.user %}
                <a href="{{ path('user_profil', {'id': app.user.id}) }}" class="mr-5 hover:text-gray-900">Profile</a>
                {% if "ROLE_ADMIN" in app.user.roles %}
                    <a href="{{ path('admin_dashboard') }}" class="mr-5 hover:text-gray-900">Dashboard</a>

                {% endif %}
            {% endif %}


        </nav>
        <a href="{{ path("app_event_index") }}" class="flex order-first lg:w-1/3 lg:order-none lg:w-1/5 title-font font-medium items-center text-gray-100 lg:items-center lg:justify-center mb-4 md:mb-0">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" class="w-10 h-10 text-white p-2 bg-indigo-500 rounded-full" viewBox="0 0 24 24">
                <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"></path>
            </svg>
            <span class="animate__animated animate__bounce ml-3 text-xl">Sortir
               <span {{ stimulus_controller('symfony/ux-typed', {
                   strings: ["!", "entre potes !", "en boîte !", "entre filles 💖", "de ma chambre .", "prendre l'air !",
                    "avec des amis !", "faire du sport !", "boire un café !", "profiter du soleil !", "voir un film !", "marcher en forêt !", "danser toute la nuit !",
                       "courir au parc !", "faire du shopping !" ],
                   smartBackspace: true,
                   startDelay: 0,
                   typeSpeed: 30,
                   backSpeed: 30,
                   backDelay: 2000,
                   loop: true,
                   showCursor: true,
                   cursorChar: '|'}) }}></span>
            </span>
        </a>
        <div class="lg:w-1/3 inline-flex lg:justify-end ml-5 lg:ml-0">
            {% if app.user %}
                <form method="POST" action="{{ path('app_logout') }}">
                    <button type="submit" class="inline-flex items-center bg-[#9693EC] border-0 py-1 px-3 focus:outline-none hover:bg-[#7A77D9] rounded text-white mt-4 md:mt-0">
                        Déconnexion
                        <svg fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" class="w-4 h-4 ml-1" viewBox="0 0 24 24">
                            <path d="M5 12h14M12 5l7 7-7 7"></path>
                        </svg>
                    </button>
                </form>
            {% endif %}
            {% if not app.user %}
                <form method="GET" action="{{ path('app_login') }}">
                    <button type="submit" class="inline-flex items-center bg-[#9693EC] border-0 py-1 px-3 focus:outline-none hover:bg-[#7A77D9] rounded text-white mt-4 md:mt-0">
                        Connexion
                        <svg fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" class="w-4 h-4 ml-1" viewBox="0 0 24 24">
                            <path d="M5 12h14M12 5l7 7-7 7"></path>
                        </svg>
                    </button>
                </form>
            {% endif %}

        </div>
    </div>
</header>