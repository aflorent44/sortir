{% extends 'base.html.twig' %}

{% block title %}Erreur {{ status_code }}{% endblock %}

{% block body %}
    <div class="flex flex-col items-center justify-center h-screen bg-gray-100 text-gray-700">
        <h1 class="text-6xl font-bold">{{ status_code }}</h1>
        <p class="text-2xl mt-4">{{ status_text }}</p>
        <p class="mt-2">Oups ! Une erreur s'est produite.</p>
        <a href="{{ path('app_event_index') }}" class="mt-4 px-4 py-2 bg-blue-500 text-white rounded">Retour à l'accueil</a>
    </div>
{% endblock %}
