{% extends 'base.html.twig' %}

{% block title %}Nouvel Événement{% endblock %}

{% block body %}
    <div class="max-w-2xl mx-auto bg-white p-6 rounded-lg shadow-md mt-6">
        <h1 class="text-4xl font-bold text-center mb-6">Créer un Nouvel Événement</h1>

        <div class="mb-6">
            {{ include('event/_form.html.twig') }}
        </div>

        <div class="text-center">
            <a href="{{ path('app_event_index') }}" class="text-blue-500 hover:underline text-lg">← Retour à la liste</a>
        </div>
    </div>
{% endblock %}