{% extends 'base.html.twig' %}

{% block title %}Sortir - Profil{% endblock %}

{% block body %}
    <div>
        <h1>{{ controller_name }} : </h1>
        <br>
        <ul>
            <li>{{ user.campus.name }}</li>
            <li>{{ user.firstName }}</li>
            <li>{{ user.name }}</li>
            <li>{{ user.phoneNumber }}</li>
            <li>{{ user.email }}</li>
            {% if app.user.imageProfile %}
            <li>
                <img src="{{ asset('uploads/images/' ~ app.user.imageProfile) }}" alt="Photo de profil">
            </li>
            {% endif %}
        </ul>
        <br>
        <a href="{{ path("user_update_profil", {'id': app.user.id}) }}" class="mr-5 hover:text-gray-900">Modifier</a>
    </div>
{% endblock %}
