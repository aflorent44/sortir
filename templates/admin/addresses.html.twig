{% extends 'base.html.twig' %}

{% block title %}Sortir ! - Lieux{% endblock %}

{% block body %}
    <h1 class="text-2xl font-bold mb-4">Liste des lieux</h1>
    <div class="flex">
        <a class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 text-3xl lg:text-lg" href="{{ path('app_address_new') }}">Nouvelle Adresse</a>
    </div>
    <table class="min-w-full border-r-2 border border-gray-600">
        <thead>
        <tr class="bg-gray-200">
            <th class="border border-gray-600 px-4 py-2">ID</th>
            <th class="border border-gray-600 px-4 py-2">Nom</th>
            <th class="border border-gray-600 px-4 py-2">adresse</th>
            <th class="border border-gray-600 px-4 py-2">Ville</th>
        </tr>
        </thead>
        <tbody>
        {% for address in addresses %}
            <tr class=" bg-gray-100 hover:bg-gray-50">
                <td class="border border-gray-600 px-4 py-2">{{ address.name }}</td>
                <td class="border border-gray-600 px-4 py-2">{{ address.street}}</td>
                <td class="border border-gray-600 px-4 py-2">{{ address.city}}</td>
                <td class="border border-gray-300 px-4 py-2">
                    <a href="{{ path("app_address_edit", {id: address.id}) }}" class="text-blue-500 hover:underline">Modifier</a>
                    <form action="{{ path("app_address_delete", { id: address.id } ) }}" method="POST">
                        <input type="submit" class="text-red-500 hover:underline" onclick="return confirm('Supprimer cette adresse ?')" value="Supprimer">
                    </form>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5" class="text-center border border-gray-300 px-4 py-2">Aucune adresse trouvée</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    </table>

{% endblock %}